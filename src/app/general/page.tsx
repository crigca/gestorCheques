import GeneralPanel from "@/app/components/GeneralPanel";

/**
 * @description Página general que muestra el panel de administración.
 * Renderiza el componente `GeneralPanel`.
 * @returns {JSX.Element} Componente JSX de la página general.
 */
export default function GeneralPage() {
  return (
    <div>
      <GeneralPanel />
    </div>
  );
}
