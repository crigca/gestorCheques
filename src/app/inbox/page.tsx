import Inbox from "../components/boxes/Inbox";

/**
 * @description Página de la bandeja de entrada (`Inbox`).
 * Renderiza el componente `Inbox` para mostrar los cheques entrantes.
 * @returns {JSX.Element} Componente JSX de la página de bandeja de entrada.
 */
export default function InboxPage() {
  return (
    <div>
      <Inbox />
    </div>
  );
}
