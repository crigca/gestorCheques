import Outbox from "../components/boxes/Outbox";

/**
 * @description Página de la bandeja de salida (`Outbox`).
 * Renderiza el componente `Outbox` para mostrar los cheques salientes.
 * @returns {JSX.Element} Componente JSX de la página de bandeja de salida.
 */
export default function OutboxPage() {
  return (
    <div>
      <Outbox />
    </div>
  );
}
